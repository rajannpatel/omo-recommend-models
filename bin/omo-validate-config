#!/usr/bin/env node
import {
  runValidateConfigCli,
} from "../lib/cli/validate-config.js";

runValidateConfigCli()
  .then((code) => {
    process.exitCode = code;
  })
  .catch((error) => {
    console.error(error.message);
    process.exitCode = 1;
  });
